import { Outlet } from "react-router-dom";

const EnterMarksWrapper = () => {
  return (
    <div className="enter-marks-wrapper">
      <Outlet />
    </div>
  );
};

export default EnterMarksWrapper;
